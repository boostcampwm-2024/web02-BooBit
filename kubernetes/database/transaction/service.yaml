apiVersion: v1
kind: Service
metadata:
  name: transaction-db
  labels:
    app: transaction-db
spec:
  type: ClusterIP
  selector:
    app: transaction-db
  ports:
    - port: 27017
      targetPort: 27017
