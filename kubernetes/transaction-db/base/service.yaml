apiVersion: v1
kind: Service
metadata:
  name: transaction-db
  labels:
    app: transaction-db
spec:
  type: ClusterIP
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app: transaction-db
