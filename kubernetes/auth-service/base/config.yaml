apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
data:
  NODE_ENV: 'production'
  DATABASE_URL: 'mysql://${DB_USER}:${DB_PASSWORD}@auth-db:3306/${DB_SCHEMA}'
  REDIS_URL: 'redis://session-db:6379'
